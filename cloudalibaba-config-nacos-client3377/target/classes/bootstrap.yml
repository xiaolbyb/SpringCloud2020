server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #服务注册中心地址
      config:
        server-addr: localhost:8848 #配置中心地址
        file-extension: yaml #指定yaml格式的配置
        #name路径nacos-config-client-dev.yml
#        group: TEST_GROUP
        group: TEST_GROUP
        namespace: 322161d0-0560-4300-8602-c3e56c3836c2
